import { Command } from 'commander';
import { OpticCliConfig } from '../../config';
export declare const registerApiAdd: (cli: Command, config: OpticCliConfig) => void;
type ApiAddActionOptions = {
    historyDepth: string;
    web: boolean;
    all: boolean;
    startCommit?: string;
};
export declare const getApiAddAction: (config: OpticCliConfig) => (path_to_spec: string | undefined, options: ApiAddActionOptions) => Promise<void>;
export {};
//# sourceMappingURL=add.d.ts.map