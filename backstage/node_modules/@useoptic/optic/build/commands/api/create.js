"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.registerApiCreate = void 0;
var _errorHandler = require("../../error-handler");
const usage = () => `
  optic api create <api_name>`;
const helpText = `
Example usage:
  Add an Optic API URL to a spec file.
  $ optic api create <api_name>
`;
const registerApiCreate = (cli, config) => {
  cli.command('create').configureHelp({
    commandUsage: usage
  }).addHelpText('after', helpText).description('Generate an optic url to add to your specs').argument('<name>', 'the name of the api').action((0, _errorHandler.errorHandler)(getApiCreateAction(config), {
    command: 'api-create'
  }));
};
exports.registerApiCreate = registerApiCreate;
const getApiCreateAction = config => async name => {
  console.error('api create is not supported');
  return;
};