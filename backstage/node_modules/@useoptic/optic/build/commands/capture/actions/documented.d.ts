import { ParseResult } from '../../../utils/spec-loaders';
import { CapturedInteractions } from '../sources/captured-interactions';
import { ApiCoverageCounter } from '../coverage/api-coverage';
export declare function diffExistingEndpoint(interactions: CapturedInteractions, parseResult: Exclude<ParseResult, {
    version: '2.x.x';
}>, coverage: ApiCoverageCounter, endpoint: {
    path: string;
    method: string;
}, options: {
    update?: 'documented' | 'interactive' | 'automatic';
    verbose: boolean;
}): Promise<{
    patchSummaries: string[];
    hasDiffs: boolean;
}>;
//# sourceMappingURL=documented.d.ts.map