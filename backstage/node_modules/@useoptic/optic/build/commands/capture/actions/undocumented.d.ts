import { ParseResult } from '../../../utils/spec-loaders';
import { CapturedInteraction, CapturedInteractions } from '../sources/captured-interactions';
import { PathInference } from '../operations/path-inference';
export declare function promptUserForPathPattern(interactions: CapturedInteractions, pathInference: PathInference, options: {
    update: 'interactive' | 'automatic';
}): Promise<{
    interactions: CapturedInteraction[];
    endpointsToAdd: {
        method: string;
        path: string;
    }[];
    ignorePaths: {
        method: string;
        path: string;
    }[];
}>;
export declare function documentNewEndpoint(interactions: CapturedInteraction[], parseResult: Exclude<ParseResult, {
    version: '2.x.x';
}>, endpoint: {
    method: string;
    path: string;
}): Promise<void>;
//# sourceMappingURL=undocumented.d.ts.map