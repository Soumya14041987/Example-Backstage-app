import { ParseResult } from '../../../utils/spec-loaders';
import { SpecPatch } from '../patches/patchers/spec/patches';
import { UnpatchableDiff } from './patchers/shapes/diff';
export type GroupedUnpatchableDiff = Omit<UnpatchableDiff, 'example'> & {
    examples: any[];
};
export declare class EndpointPatchSummaries {
    private parseResult;
    private options;
    private patches;
    constructor(parseResult: ParseResult, options: {
        update?: 'documented' | 'interactive' | 'automatic';
        verbose: boolean;
    });
    addPatch(patchOrDiff: SpecPatch | GroupedUnpatchableDiff): void;
    getPatchSummaries(): string[];
}
//# sourceMappingURL=summaries.d.ts.map