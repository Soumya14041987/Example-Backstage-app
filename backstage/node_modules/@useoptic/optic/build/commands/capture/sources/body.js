"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.CapturedBody = void 0;
class CapturedBody {
  static from(stringValue, contentType = null) {
    return {
      contentType,
      body: stringValue,
      size: (stringValue === null || stringValue === void 0 ? void 0 : stringValue.length) || 0
    };
  }
  static fromJSON(json, ...rest) {
    const asJsonString = JSON.stringify(json);
    return CapturedBody.from(asJsonString, 'application/json');
  }
  static body(body) {
    return body.body;
  }
  static async json(body) {
    const text = await CapturedBody.text(body);
    return JSON.parse(text);
  }
  static async text(body) {
    return body.body;
  }
}
exports.CapturedBody = CapturedBody;