import type { GroupedDiffs } from '@useoptic/openapi-utilities/build/openapi3/group-diff';
import { SpecInput } from './common';
type ChangedNode = {
    name: string;
    change: 'added' | 'removed' | 'changed' | null;
    attributes: {
        key: string;
        before: any;
        after: any;
        change: 'added' | 'changed' | 'removed';
    }[];
};
type OperationChangelog = ChangedNode & {
    parameters: ChangedNode[];
    requestBody?: ChangedNode & {
        contentTypes: ChangedNode[];
    };
    responses: (ChangedNode & {
        contentTypes: ChangedNode[];
        headers: ChangedNode[];
    })[];
};
type JsonChangelog = {
    operations: OperationChangelog[];
};
export declare function jsonChangelog(specs: SpecInput, groupedChanges: GroupedDiffs): JsonChangelog;
export {};
//# sourceMappingURL=json-changelog.d.ts.map