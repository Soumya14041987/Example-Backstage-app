import { ObjectDiff, RuleResult, Severity, GetSourcemapOptions } from '@useoptic/openapi-utilities';
import type { GroupedDiffs } from '@useoptic/openapi-utilities/build/openapi3/group-diff';
import { ParseResult } from '../../../utils/spec-loaders';
export declare function terminalChangelog(specs: {
    from: Exclude<ParseResult, {
        version: '2.x.x';
    }>;
    to: Exclude<ParseResult, {
        version: '2.x.x';
    }>;
}, groupedDiffs: GroupedDiffs, comparison: {
    results: RuleResult[];
    diffs: ObjectDiff[];
}, options: GetSourcemapOptions & {
    path: string;
    check: boolean;
    inCi: boolean;
    output: 'pretty' | 'plain';
    verbose: boolean;
    severity: Severity;
    previewDocsLink?: string | null;
}): Generator<string>;
//# sourceMappingURL=terminal-changelog.d.ts.map