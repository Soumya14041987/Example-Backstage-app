import { CompareSpecResults } from '@useoptic/openapi-utilities';
import { ParseResult } from '../../utils/spec-loaders';
import { ConfigRuleset, OpticCliConfig } from '../../config';
import { GroupedDiffs } from '@useoptic/openapi-utilities/build/openapi3/group-diff';
export declare function setGenerateContext(fn: (file: string) => any): void;
export declare function compute([baseFile, headFile]: [ParseResult, ParseResult], config: OpticCliConfig, options: {
    standard?: string;
    check: boolean;
    path: string | null;
}): Promise<{
    standard: ConfigRuleset[];
    warnings: string[];
    specResults: CompareSpecResults;
    changelogData: GroupedDiffs;
    checks: {
        total: number;
        passed: number;
        exempted: number;
        failed: {
            info: number;
            error: number;
            warn: number;
        };
    };
}>;
//# sourceMappingURL=compute.d.ts.map