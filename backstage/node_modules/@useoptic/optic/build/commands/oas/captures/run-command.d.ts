/// <reference types="node" />
import { ChildProcessWithoutNullStreams } from 'child_process';
import { createCommandFeedback } from '../reporters/feedback';
export declare class RunCommand {
    private proxyUrl;
    private feedback;
    private options;
    constructor(proxyUrl: string, feedback: ReturnType<typeof createCommandFeedback>, options: {
        reverseProxy: boolean;
    });
    completed: boolean;
    process: ChildProcessWithoutNullStreams | undefined;
    run(command: string): Promise<{
        exitCode: number;
    }>;
    kill(): Promise<void>;
}
//# sourceMappingURL=run-command.d.ts.map