import { createCommandFeedback } from '../reporters/feedback';
export declare class SystemProxy {
    private proxyUrl;
    private feedback;
    constructor(proxyUrl: string, feedback: ReturnType<typeof createCommandFeedback>);
    private stopCommand;
    start(device: string | undefined): Promise<void>;
    stop(): Promise<void>;
}
export declare function chooseInterfaceMac(): Promise<string>;
//# sourceMappingURL=system-proxy.d.ts.map