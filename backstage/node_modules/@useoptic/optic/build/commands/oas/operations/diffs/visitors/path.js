"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.visitPath = visitPath;
var _types = require("../../../../capture/patches/patchers/spec/types");
function* visitPath(path, specOption, context) {
  if (specOption.none) {
    yield {
      kind: _types.OperationDiffResultKind.UnmatchedPath,
      subject: path
    };
  }

  // TODO: match path's parameters against those documented
  // let parameterComponents = PathComponents.fromPath(path).filter(
  //   PathComponent.isTemplate
  // );
  // const spec = specOption.unwrap()
  // spec.parameters
}