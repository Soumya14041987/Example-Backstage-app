/// <reference types="node" />
import { WriteStream } from 'tty';
type ObservedOperation = {
    pathPattern: string;
    method: string;
};
export declare function updateReporter(stream: WriteStream, cwd: string): Promise<{
    capturedInteraction({ path, method }: {
        path: string;
        method: string;
    }): void;
    matchedInteraction(op: ObservedOperation): void;
    patch(op: ObservedOperation, capturedPath: string, description: string): void;
    fileUpdated(path: string): void;
    finish(): void;
}>;
export {};
//# sourceMappingURL=update.d.ts.map