import { Command } from 'commander';
import { createCommandFeedback } from './reporters/feedback';
import { OpticCliConfig } from '../../config';
type VerifyOptions = {
    exit0?: boolean;
    har?: string;
    postman?: string;
    upload?: boolean;
    message?: string;
};
export declare function verifyCommand(config: OpticCliConfig): Command;
export declare function runVerify(specPath: string, options: VerifyOptions, config: OpticCliConfig, feedback: ReturnType<typeof createCommandFeedback>, internalOptions?: {
    printCoverage: boolean;
}): Promise<void>;
export {};
//# sourceMappingURL=verify.d.ts.map