"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.registerRulesetUpload = void 0;
var _errorHandler = require("../../error-handler");
const registerRulesetUpload = (cli, config) => {
  cli.command('upload').configureHelp({
    commandUsage: () => `OPTIC_TOKEN=your_token_here optic ruleset upload <path_to_ruleset>`
  }).description('Upload a custom ruleset to optic cloud').addHelpText('after', `
This command also requires a token to be provided via the environment variable OPTIC_TOKEN. Generate an optic token at https://app.useoptic.com.`).argument('<path_to_ruleset>', 'the path to the javascript ruleset file to upload, typically "./build/main.js".').option('--organization-id <organization-id>', 'specify an organization to add this to').action((0, _errorHandler.errorHandler)(getUploadAction(config), {
    command: 'ruleset-upload'
  }));
};
exports.registerRulesetUpload = registerRulesetUpload;
const getUploadAction = config => async (filePath, options) => {
  console.log('Ruleset upload is no longer supported');
  return;
};