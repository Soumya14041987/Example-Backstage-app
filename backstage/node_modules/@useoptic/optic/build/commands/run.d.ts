import { Command } from 'commander';
import { OpticCliConfig } from '../config';
import { CustomUploadFn } from '../types';
declare const severities: readonly ["none", "error"];
export declare function registerRunCommand(cli: Command, config: OpticCliConfig, options: {
    customUpload?: CustomUploadFn;
}): void;
type RunActionOptions = {
    ignore?: string;
    severity: (typeof severities)[number];
    includeGitIgnored: boolean;
};
export declare const getRunAction: (config: OpticCliConfig, customOptions: {
    customUpload?: CustomUploadFn;
}) => (matchArg: string | undefined, options: RunActionOptions) => Promise<void>;
export {};
//# sourceMappingURL=run.d.ts.map