"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");
Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.logger = void 0;
var _loglevel = _interopRequireDefault(require("loglevel"));
const validLogLevels = new Set(['trace', 'debug', 'info', 'warn', 'error', 'silent']);
const logLevel = process.env.LOG_LEVEL;
if (!logLevel && process.env.ENVIRONMENT === 'test') {
  _loglevel.default.setLevel('silent');
} else if (logLevel && validLogLevels.has(logLevel)) {
  _loglevel.default.setLevel(logLevel);
} else {
  _loglevel.default.setLevel('info');
}
_loglevel.default.setLevel(_loglevel.default.getLevel()); // Applies the plugin

const logger = exports.logger = _loglevel.default;