"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");
Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.commandSplitter = commandSplitter;
exports.resolveRelativePath = resolveRelativePath;
var _path = _interopRequireDefault(require("path"));
var _crosspath = require("crosspath");
// commandSplitter() splits a command and its args into a SpawnCommand--in a naive fashion. it
// assumes that `command` contains a single command, rather than something more complex. since
// there is no validation or safety checks performed on `command` exercise caution where you
// use this.
function commandSplitter(command) {
  return {
    cmd: command.split(' ')[0],
    args: command.split(' ').slice(1)
  };
}

// returns the relative path between file and rootPath
function resolveRelativePath(rootPath, file) {
  return (0, _crosspath.normalize)(_path.default.relative(rootPath, _path.default.resolve(file)));
}