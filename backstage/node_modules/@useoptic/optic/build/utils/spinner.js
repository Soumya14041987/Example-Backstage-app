"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");
Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getSpinner = void 0;
var _ora = _interopRequireDefault(require("ora"));
var _logger = require("../logger");
const spinnerIsDisabled = process.env.ENVIRONMENT === 'test' || _logger.logger.getLevel() === 5;
const getSpinner = options => {
  if (spinnerIsDisabled) {
    const text = typeof options === 'string' ? options : (options === null || options === void 0 ? void 0 : options.text) ?? '';
    _logger.logger.info(text);
    return null;
  } else {
    return (0, _ora.default)(options);
  }
};
exports.getSpinner = getSpinner;