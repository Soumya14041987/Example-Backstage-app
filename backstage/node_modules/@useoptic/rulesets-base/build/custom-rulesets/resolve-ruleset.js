"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.resolveRuleset = void 0;
const ajv_1 = __importDefault(require("ajv"));
const load_ruleset_1 = require("./load-ruleset");
const ajv = new ajv_1.default();
async function resolveRuleset(ruleset, rulesetPath) {
    const rulesetMod = (0, load_ruleset_1.loadRuleset)(rulesetPath);
    const validate = ajv.compile(rulesetMod.default.configSchema);
    const valid = validate(ruleset.config);
    if (valid) {
        return rulesetMod.default.rulesetConstructor(ruleset.config);
    }
    else {
        return `Ruleset ${ruleset.name} had configuration errors:\n${ajv.errorsText(validate.errors)}`;
    }
}
exports.resolveRuleset = resolveRuleset;
//# sourceMappingURL=resolve-ruleset.js.map