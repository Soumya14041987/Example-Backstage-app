import { OpenApiKind, Result } from '@useoptic/openapi-utilities';
import { EndpointNode, RequestNode, NodeDetail } from './rule-runner-types';
import { Rule, Ruleset } from '../rules';
import { OpenAPIDocument } from '../types';
export declare const runRequestRules: ({ specificationNode, operationNode, requestNode, rules, customRuleContext, beforeApiSpec, afterApiSpec, }: {
    specificationNode: NodeDetail<OpenApiKind.Specification>;
    operationNode: EndpointNode;
    requestNode: RequestNode;
    rules: (Ruleset | Rule)[];
    customRuleContext: any;
    beforeApiSpec: OpenAPIDocument;
    afterApiSpec: OpenAPIDocument;
}) => Result[];
//# sourceMappingURL=request.d.ts.map