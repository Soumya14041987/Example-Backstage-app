import { OpenApiKind, Result } from '@useoptic/openapi-utilities';
import { NodeDetail } from './rule-runner-types';
import { Rule, Ruleset } from '../rules';
import { OpenAPIDocument } from '..';
export declare const runSpecificationRules: ({ specificationNode, rules, customRuleContext, beforeApiSpec, afterApiSpec, }: {
    specificationNode: NodeDetail<OpenApiKind.Specification>;
    rules: (Ruleset | Rule)[];
    customRuleContext: any;
    beforeApiSpec: OpenAPIDocument;
    afterApiSpec: OpenAPIDocument;
}) => Result[];
//# sourceMappingURL=specification.d.ts.map