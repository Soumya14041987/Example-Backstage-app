"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.PropertyRule = void 0;
const openapi_utilities_1 = require("@useoptic/openapi-utilities");
class PropertyRule {
    constructor(config) {
        // this could be invoked via javascript so we still to check
        if (!config.name) {
            throw new Error('Expected a name in PropertyRule');
        }
        if (!config.rule) {
            throw new Error('Expected a rule definition in PropertyRule');
        }
        this.name = config.name;
        this.docsLink = config.docsLink;
        this.matches = config.matches;
        this.rule = config.rule;
        this.type = 'property-rule';
        this.severity = config.severity && (0, openapi_utilities_1.textToSev)(config.severity);
    }
    static isInstance(v) {
        return v?.type === 'property-rule';
    }
}
exports.PropertyRule = PropertyRule;
//# sourceMappingURL=property-rule.js.map