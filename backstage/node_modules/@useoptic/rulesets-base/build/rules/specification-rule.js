"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.SpecificationRule = void 0;
const openapi_utilities_1 = require("@useoptic/openapi-utilities");
class SpecificationRule {
    constructor(config) {
        // this could be invoked via javascript so we still to check
        if (!config.name) {
            throw new Error('Expected a name in SpecificationRule');
        }
        if (!config.rule) {
            throw new Error('Expected a rule definition in SpecificationRule');
        }
        this.name = config.name;
        this.docsLink = config.docsLink;
        this.matches = config.matches;
        this.rule = config.rule;
        this.type = 'specification-rule';
        this.severity = config.severity && (0, openapi_utilities_1.textToSev)(config.severity);
    }
    static isInstance(v) {
        return v?.type === 'specification-rule';
    }
}
exports.SpecificationRule = SpecificationRule;
//# sourceMappingURL=specification-rule.js.map