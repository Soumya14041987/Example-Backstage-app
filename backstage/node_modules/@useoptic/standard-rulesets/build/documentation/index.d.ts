import { Ruleset } from '@useoptic/rulesets-base';
import { SeverityText } from '@useoptic/openapi-utilities';
import { appliesWhen } from './constants';
export declare class DocumentationRuleset extends Ruleset {
    static fromOpticConfig(config: unknown): Promise<DocumentationRuleset | string>;
    constructor(config: {
        required_on: (typeof appliesWhen)[number];
        require_property_descriptions?: boolean;
        require_operation_summary?: boolean;
        require_operation_description?: boolean;
        require_operation_id?: boolean;
        docsLink?: string;
        matches?: Ruleset['matches'];
        severity?: SeverityText;
    });
}
//# sourceMappingURL=index.d.ts.map