/**
 * Take the first value or the first value to pass predicate from an async iterable
 */
export declare function first<T>(predicate?: (t: T) => boolean): (source: AsyncIterable<T>) => AsyncGenerator<T, void, unknown>;
