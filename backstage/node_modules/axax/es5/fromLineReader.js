"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var subject_1 = require("./subject");
function fromLineReader(lineReader) {
    var subject = new subject_1.Subject();
    lineReader.on("line", function (line) { return subject.onNext(line); });
    lineReader.on("close", function () { return subject.onCompleted(); });
    subject.finally(function () { return lineReader.close(); });
    return subject.iterator;
}
exports.fromLineReader = fromLineReader;
