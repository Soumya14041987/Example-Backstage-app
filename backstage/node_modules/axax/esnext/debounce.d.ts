/**
 * Debounce an async iterator. Only emit values when no new value has been received during the provided timer duration.
 */
export declare function debounce<T>(timer: (value: T) => Promise<void>): (source: AsyncIterable<T>) => AsyncGenerator<any, void, unknown>;
