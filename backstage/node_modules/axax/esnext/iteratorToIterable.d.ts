/**
 * Helper function to turn an iterator into an iterable
 *
 * @param iterator An iteratable
 */
export declare function iteratorToIterable<T>(iterator: AsyncIterator<T>): AsyncGenerator<T, void, unknown>;
